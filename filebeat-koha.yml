---
- hosts: koha
  become: true

  tasks:
  - name: Write apache filebeat configuration
    ansible.builtin.template:
      src: templates/filebeat/inputs.d/koha.yml.j2
      dest: /etc/filebeat/inputs.d/koha.yml
      owner: root
      group: root
